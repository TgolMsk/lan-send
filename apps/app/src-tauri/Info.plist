<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Merged into the generated macOS Info.plist by `tauri build` (iOS uses Info.ios.plist). -->
    <!-- Only standard TLS (rustls) is used: exempt from export documentation. -->
    <key>NSHumanReadableCopyright</key>
    <string>© 2026 Wang Sheng</string>
    <key>ITSAppUsesNonExemptEncryption</key>
    <false/>
    <!-- macOS 15+ asks before an app may talk to the local network. -->
    <!-- Shown by macOS the first time the sandboxed app writes a received file into ~/Downloads. -->
    <key>NSDownloadsFolderUsageDescription</key>
    <string>LanSend needs access to your Downloads folder to save the files other devices send you. For example, when you send a photo from your iPhone to this Mac, LanSend writes it to your Downloads folder so you can open it in Finder. You can choose a different folder in Settings.</string>
    <key>NSLocalNetworkUsageDescription</key>
    <string>LanSend needs access to your local network to find your other devices and send files to them directly. For example, when you choose a photo and pick your Mac or iPhone in the device list, LanSend discovers that device on your Wi‑Fi and sends the photo straight to it, with no server in between.</string>
</dict>
</plist>
